# 项目约定

## 语言
- 所有对话、注释、提交信息均使用中文

## 输出目录
- 环境变量 `OUTPUT_ROOT` 指定输出根目录，运行脚本时必须注入此变量，值：`C:\Users\Noah\Documents\vscode`（本项目根目录）

## Git 规则
- 所有 git 操作基于项目根目录仓库（C:\Users\Noah\Documents\vscode）
- 不要自动 commit，等我明确要求后再提交
- 创建 commit 或 push 前必须先询问确认
- commit message 使用中文
- 注意：Aetherion 和 .claude/skills 有独立的 git 仓库，默认不要操作它们
