interface:
  display_name: "Doubao Image"
  short_description: "Run Doubao Seedream text-to-image and image-to-image scripts with OpenClaw-managed ARK_API_KEY."
  default_prompt: "Use $doubao-image-generation to run the workspace Doubao image scripts with the user's prompt, source image, size, and output path requirements."
