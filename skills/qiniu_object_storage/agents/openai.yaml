interface:
  display_name: "Qiniu Object Storage"
  short_description: "Upload local files and return public or signed delivery links."
  default_prompt: "Use $qiniu_object_storage when a local file already exists and the user needs a public URL or a signed download link."
